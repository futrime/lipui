<?xml version="1.0" encoding="utf-8"?>

<Window
    x:Class="LipUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LipUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBar">

            <Image Source="Images/WindowIcon.png"
                   HorizontalAlignment="Left" 
                   Width="16" Height="16" 
                   Margin="8,0"/>

            <TextBlock x:Name="AppTitleTextBlock" Text="i18n.lipui.title"
                       TextWrapping="NoWrap"
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       VerticalAlignment="Center"
                       Margin="28,0,0,0"/>

        </Grid>

        <NavigationView 
            Grid.Row="1"
            x:Name="NavView"
            PaneDisplayMode="LeftCompact"
            Loaded="NavView_Loaded"
            ItemInvoked="NavView_ItemInvoked"
            BackRequested="NavView_BackRequested">

            <!--<NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="#10FFFFFF" />
            </NavigationView.Resources>-->

            <NavigationView.MenuItems>

                <NavigationViewItem Icon="Home" Tag="{x:Bind NavigationPage.HomePage}" Content="i18n.home"/>
                <NavigationViewItem Icon="AllApps" Tag="{x:Bind NavigationPage.LocalPackagePage}" Content="i18n.local_packages"/>
                <NavigationViewItem Icon="Add" Tag="{x:Bind NavigationPage.PackToothPage}" Content="i18n.pack_a_new_tooth"/>
                <NavigationViewItem Icon="Find" Tag="{x:Bind NavigationPage.IndexPage}" Content="i18n.teeth_index"/>

            </NavigationView.MenuItems>

            <Frame x:Name="ContentFrame" IsTabStop="True" NavigationFailed="ContentFrame_NavigationFailed" CornerRadius="5"/>

        </NavigationView>
    </Grid>

</Window>
